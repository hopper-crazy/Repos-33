# ============================================================
# GET TRANSPORT REQUEST
# ============================================================

def get_transport_request(session, base_url, tr_number):

    url = (
        f"{base_url}/sap/bc/adt/cts/"
        f"transportrequests"
    )


    # --------------------------------------------------------
    # Ask user for TR number
    # --------------------------------------------------------

    tr_number = input("Enter Transport Request number: ").strip()

